# 🚀 คู่มือการ Deploy ไป Vercel

## ขั้นตอนการ Deploy

### 1. เตรียมโปรเจค
โปรเจคนี้พร้อม deploy แล้ว! มีไฟล์ `vercel.json` ที่จำเป็นอยู่แล้ว

### 2. สร้างบัญชี Vercel
1. ไปที่ https://vercel.com
2. Sign up ด้วย GitHub account
3. อนุญาตให้ Vercel เข้าถึง GitHub repository

### 3. Deploy โปรเจค

#### วิธีที่ 1: Deploy ผ่าน Vercel Dashboard (แนะนำ)
1. ไปที่ https://vercel.com/new
2. เลือก "Import Git Repository"
3. เลือก repository: `Kimzimi/elah.pao`
4. กดปุ่ม "Import"
5. ตั้งค่าดังนี้:
   - **Framework Preset**: Other
   - **Root Directory**: `./`
   - **Build Command**: (ปล่อยว่างไว้)
   - **Output Directory**: (ปล่อยว่างไว้)

6. คลิก "Deploy"

#### วิธีที่ 2: Deploy ผ่าน Vercel CLI
```bash
# ติดตั้ง Vercel CLI
npm install -g vercel

# Login
vercel login

# Deploy
vercel

# Deploy เป็น Production
vercel --prod
```

### 4. ตั้งค่า Environment Variables (สำคัญ!)

หลังจาก deploy แล้ว ต้องตั้งค่า Environment Variables ใน Vercel Dashboard:

1. ไปที่ Project Settings → Environment Variables
2. เพิ่ม Variables ดังนี้:

```
NODE_ENV = production
```

**หมายเหตุ:** ข้อมูลอีเมลและรหัสผ่าน App Password อยู่ในไฟล์ `server.js` แล้ว
หากต้องการเปลี่ยน ให้แก้ไขตรงบรรทัด:
```javascript
const EMAIL_USER = 'elah.pao@gmail.com';
const EMAIL_PASS = 'lunt pjfa xlrb wunj';
const SELLER_EMAIL = 'elah.pao@gmail.com';
```

### 5. Redeploy (ถ้าจำเป็น)
หลังจากเพิ่ม Environment Variables แล้ว:
1. ไปที่ Deployments
2. คลิกที่ deployment ล่าสุด
3. คลิก "..." → "Redeploy"

### 6. ทดสอบเว็บไซต์
เว็บไซต์จะพร้อมใช้งานที่:
```
https://your-project-name.vercel.app
```

**ทดสอบหน้าต่างๆ:**
- หน้าหลัก: `/`
- หน้าชำระเงิน: `/payment.html`
- Dashboard: `/dashboard`
- Admin Login: `/admin-login`
- Admin Panel: `/admin`

### 7. ตั้งค่า Custom Domain (ถ้าต้องการ)
1. ไปที่ Project Settings → Domains
2. เพิ่ม domain ของคุณ
3. ตั้งค่า DNS ตามที่ Vercel แนะนำ

## ⚠️ ข้อควรระวัง

### 1. ไฟล์ที่ต้องอัพโหลดเอง
หลังจาก deploy แล้ว ต้องอัพโหลดไฟล์เหล่านี้ผ่าน Admin Panel:

- **QR Code**: ไปที่ `/admin` → แท็บ "ข้อมูลการชำระเงิน" → อัพโหลด QR Code

### 2. ข้อมูลที่ถูกเก็บ
- ออเดอร์จะถูกเก็บในไฟล์ `data/orders.json`
- การตั้งค่าจะถูกเก็บในไฟล์ `data/settings.json`
- รูปสลิปจะถูกเก็บในโฟลเดอร์ `public/uploads/`

**หมายเหตุ:** Vercel ไม่รองรับการเก็บไฟล์แบบถาวร (Serverless)
ถ้าต้องการเก็บข้อมูลแบบถาวร แนะนำให้ใช้:
- Database: MongoDB, PostgreSQL, MySQL
- File Storage: AWS S3, Cloudinary, Vercel Blob

### 3. อีเมลแจ้งเตือน
- ระบบจะส่งอีเมลแจ้งเตือนไปที่ `elah.pao@gmail.com` เมื่อมีออเดอร์ใหม่
- ตรวจสอบว่า Gmail App Password ยังใช้งานได้อยู่

## 🔄 การอัพเดทโปรเจค

เมื่อมีการแก้ไขโค้ด:

```bash
# Commit changes
git add .
git commit -m "Update: คำอธิบายการแก้ไข"

# Push to GitHub
git push origin main
```

Vercel จะ auto-deploy ทันทีที่มีการ push ไป GitHub!

## 📱 หลังจาก Deploy สำเร็จ

1. **ทดสอบการสั่งซื้อ:**
   - เข้าหน้าหลัก
   - เลือกเมนู
   - กรอกข้อมูล
   - ชำระเงิน
   - ตรวจสอบว่าได้รับอีเมล

2. **ทดสอบ Admin Panel:**
   - เข้า `/admin-login`
   - Login ด้วยรหัส: `admin123`
   - ทดสอบแก้ไขเมนู, เพิ่มวันหยุดพิเศษ

3. **ตรวจสอบ Dashboard:**
   - เข้า `/dashboard`
   - ดูว่าออเดอร์แสดงผลถูกต้อง

## 🆘 แก้ปัญหา

### ปัญหา: ไม่ได้รับอีเมล
- ตรวจสอบ Gmail App Password ใน `server.js`
- ตรวจสอบว่า email มีการเปิด "Less secure app access"

### ปัญหา: รูปภาพไม่แสดง
- อัพโหลด QR Code ใหม่ผ่าน Admin Panel
- ตรวจสอบว่าไฟล์อยู่ในโฟลเดอร์ที่ถูกต้อง

### ปัญหา: ออเดอร์หายหลัง Redeploy
- เนื่องจาก Vercel ไม่รองรับ file storage แบบถาวร
- แนะนำให้ใช้ Database จริงๆ

---

**สร้างด้วย ❤️ โดย Claude Code**
**Deploy แล้วอย่าลืมแชร์ลิงก์ให้ลูกค้านะครับ! 🎉**
