# 📧 คู่มือการตั้งค่าอีเมลสำหรับแจ้งเตือน

## วิธีตั้งค่า Gmail App Password

### ขั้นตอนที่ 1: เปิดใช้งาน 2-Step Verification

1. ไปที่ https://myaccount.google.com/security
2. เลื่อนลงไปหา "2-Step Verification"
3. คลิก "Get started" และทำตามขั้นตอน
4. ยืนยันด้วยเบอร์โทรศัพท์

### ขั้นตอนที่ 2: สร้าง App Password

1. กลับไปที่ https://myaccount.google.com/security
2. เลื่อนลงไปหา "2-Step Verification"
3. เลื่อนลงไปด้านล่างสุด หาหัวข้อ "App passwords"
4. คลิก "App passwords"
5. เลือก "Mail" และ "Other (Custom name)"
6. ตั้งชื่อ เช่น "Grilled Fish Order System"
7. คลิก "Generate"
8. **คัดลอก App Password ที่ได้** (16 ตัวอักษร ไม่มีเว้นวรรค)

### ขั้นตอนที่ 3: แก้ไขไฟล์ server.js

เปิดไฟล์ `server.js` และแก้ไขบรรทัดที่ 26-28:

```javascript
// Email Configuration
const EMAIL_USER = 'your-email@gmail.com';        // ใส่อีเมล Gmail ของคุณ
const EMAIL_PASS = 'abcd efgh ijkl mnop';         // ใส่ App Password ที่คัดลอกมา (16 ตัวอักษร)
const SELLER_EMAIL = 'seller-email@gmail.com';    // อีเมลที่จะรับการแจ้งเตือน
```

**ตัวอย่าง:**
```javascript
const EMAIL_USER = 'myshop@gmail.com';
const EMAIL_PASS = 'abcd efgh ijkl mnop';
const SELLER_EMAIL = 'owner@gmail.com';
```

**หมายเหตุ:**
- `EMAIL_USER` - อีเมล Gmail ที่ใช้ส่ง (ต้องเปิด 2-Step Verification)
- `EMAIL_PASS` - App Password 16 ตัวอักษร (ไม่ใช่รหัสผ่านปกติ)
- `SELLER_EMAIL` - อีเมลที่จะรับการแจ้งเตือนออเดอร์ใหม่

## วิธีทดสอบ

1. แก้ไขค่าใน `server.js` ตามด้านบน
2. รีสตาร์ทเซิร์ฟเวอร์: `npm start`
3. ลองสั่งสินค้าและอัพโหลดสลิป
4. ตรวจสอบอีเมล `SELLER_EMAIL` จะได้รับอีเมลแจ้งเตือน

## ตัวอย่างอีเมลที่จะได้รับ

- **หัวข้อ:** 🐟 ออเดอร์ใหม่ ORD1234567890 - ชื่อลูกค้า
- **เนื้อหา:**
  - Order ID
  - เวลาสั่ง / เวลาที่ต้องการรับ
  - ข้อมูลลูกค้า (ชื่อ, เบอร์โทร)
  - วิธีรับสินค้า (รับเองหรือจัดส่ง)
  - รายการสั่งซื้อและราคา
  - ลิงก์เปิด Dashboard
- **ไฟล์แนบ:** สลิปการโอนเงิน

## แก้ไขปัญหา

### ส่งอีเมลไม่ได้ - Error: Invalid login

**สาเหตุ:**
- ยังไม่เปิด 2-Step Verification
- App Password ไม่ถูกต้อง
- อีเมลไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่าเปิด 2-Step Verification แล้ว
2. สร้าง App Password ใหม่
3. ตรวจสอบว่าคัดลอก App Password ครบ 16 ตัวอักษร

### ส่งอีเมลได้แต่ไม่เห็นในกล่องขาเข้า

**วิธีแก้:**
1. ตรวจสอบโฟลเดอร์ Spam/Junk
2. ตรวจสอบว่า `SELLER_EMAIL` ถูกต้อง

### ส่งอีเมลช้า

**สาเหตุ:**
- Gmail มีข้อจำกัดการส่งอีเมล

**วิธีแก้:**
- ปกติแล้วจะส่งภายใน 5-10 วินาที
- ถ้าช้ามาก ลองรีสตาร์ทเซิร์ฟเวอร์

## ข้อจำกัดของ Gmail

- **ส่งได้สูงสุด:** 500 อีเมล/วัน (สำหรับ Gmail ทั่วไป)
- **ขนาดไฟล์แนบ:** ไม่เกิน 25 MB
- **ผู้รับสูงสุด:** 100 คน/อีเมล

## ทางเลือกอื่น

ถ้าต้องการส่งอีเมลจำนวนมาก สามารถใช้:
- **SendGrid** (ฟรี 100 อีเมล/วัน)
- **Mailgun** (ฟรี 5,000 อีเมล/เดือน)
- **Amazon SES** (ราคาถูก)

---

**หมายเหตุ:** อย่าเผยแพร่ App Password ให้ใครทราบ และไม่ควร commit ขึ้น Git
