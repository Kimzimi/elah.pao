# 🐟 ระบบสั่งปลาเผาออนไลน์

ระบบสั่งซื้อปลาเผาออนไลน์ที่รองรับการใช้งานทั้งมือถือและ PC พร้อมระบบชำระเงินผ่าน PromptPay QR Code และแจ้งเตือนออเดอร์ผ่าน Telegram

## ✨ ฟีเจอร์

- 📱 รองรับการใช้งานทั้งมือถือและ PC (Responsive Design)
- 🐟 เลือกจำนวนปลาเผาได้หลายแบบ
- 🍱 สั่งแบบชุดหรือแค่ปลาเดี่ยวๆ ก็ได้
- 💳 แสดง QR Code PromptPay สำหรับชำระเงิน
- 📲 ส่งออเดอร์ไปยัง Telegram อัตโนมัติ
- 🎨 UI สวยงาม ใช้งานง่าย

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
cd grilled-fish-order
npm install
```

### 2. ตั้งค่า Telegram Bot

#### สร้าง Telegram Bot:
1. เปิด Telegram และค้นหา `@BotFather`
2. ส่งคำสั่ง `/newbot`
3. ตั้งชื่อบอท เช่น `ปลาเผาออเดอร์`
4. ตั้ง username เช่น `grilled_fish_order_bot`
5. คัดลอก **Bot Token** ที่ได้

#### หา Chat ID ของคุณ:
1. เปิดแชทกับบอทที่สร้างไว้
2. ส่งข้อความอะไรก็ได้ไปหาบอท เช่น `/start`
3. เปิดเบราว์เซอร์และไปที่:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
4. หา `"chat":{"id":123456789}` - ตัวเลขนี้คือ Chat ID ของคุณ

### 3. แก้ไขไฟล์ `server.js`

เปิดไฟล์ `server.js` และแก้ไขบรรทัดที่ 8-9:

```javascript
const TELEGRAM_BOT_TOKEN = 'ใส่ Bot Token ของคุณที่นี่';
const TELEGRAM_CHAT_ID = 'ใส่ Chat ID ของคุณที่นี่';
```

### 4. แก้ไขเบอร์ PromptPay

เปิดไฟล์ `public/payment.js` และแก้ไขบรรทัดที่ 56:

```javascript
const promptPayID = '0812345678'; // เปลี่ยนเป็นเบอร์ PromptPay ของคุณ (10 หลัก)
```

### 5. เริ่มใช้งาน

```bash
npm start
```

เปิดเบราว์เซอร์และไปที่: `http://localhost:3000`

## 📖 วิธีใช้งาน

1. **เลือกเมนู**: เลือกปลาเผาหรือชุดที่ต้องการ กดปุ่ม + / - เพื่อปรับจำนวน
2. **กรอกข้อมูล**: ใส่ชื่อและเบอร์โทรศัพท์
3. **สั่งซื้อ**: กดปุ่ม "สั่งซื้อและชำระเงิน"
4. **ชำระเงิน**: สแกน QR Code PromptPay เพื่อชำระเงิน
5. **ยืนยัน**: กดปุ่ม "ยืนยันการชำระเงิน"
6. **รอรับ**: ร้านค้าจะได้รับออเดอร์ทาง Telegram ทันที

## 🛠️ โครงสร้างโปรเจค

```
grilled-fish-order/
├── public/
│   ├── index.html      # หน้าหลักสำหรับสั่งอาหาร
│   ├── payment.html    # หน้าชำระเงินและ QR Code
│   ├── style.css       # ไฟล์ CSS
│   ├── script.js       # ตรรกะหน้าหลัก
│   └── payment.js      # ตรรกะหน้าชำระเงิน
├── server.js           # Backend Server
├── package.json        # Dependencies
└── README.md           # เอกสารนี้
```

## 💡 การปรับแต่ง

### เปลี่ยนเมนูปลาเผา

แก้ไขไฟล์ `public/index.html` และ `public/script.js`:
- เพิ่มหรือลบเมนูใน HTML
- อัพเดท object `prices` และ `itemNames` ใน JavaScript

### เปลี่ยนสีธีม

แก้ไขไฟล์ `public/style.css`:
- เปลี่ยน gradient colors ใน `background`, `header`, และปุ่มต่างๆ

### เปลี่ยนพอร์ต

แก้ไขในไฟล์ `server.js` บรรทัดท้ายสุด:
```javascript
const PORT = 3000; // เปลี่ยนเป็นพอร์ตที่ต้องการ
```

## 🔒 ความปลอดภัย

- ⚠️ **อย่า commit** Bot Token และ Chat ID ขึ้น Git
- แนะนำให้ใช้ไฟล์ `.env` สำหรับเก็บข้อมูลสำคัญ
- ใช้ HTTPS เมื่อ deploy จริง

## 📦 การ Deploy

### Deploy บน Heroku, Railway, หรือ Render:
1. สร้าง Git repository
2. Push code ขึ้น
3. ตั้งค่า environment variables (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
4. Deploy!

## 🐛 แก้ไขปัญหา

### QR Code ไม่แสดง
- ตรวจสอบว่ามี internet connection
- เช็คว่าเบอร์ PromptPay ถูกต้อง (10 หลัก)

### ไม่ได้รับการแจ้งเตือนใน Telegram
- ตรวจสอบ Bot Token และ Chat ID
- ลองส่งข้อความไปหาบอทก่อน
- เช็ค console log ว่ามี error อะไร

### หน้าเว็บไม่โหลด
- ตรวจสอบว่า server กำลังรันอยู่
- เช็คพอร์ตว่าถูกใช้งานอยู่หรือไม่

## 📄 License

MIT License - สามารถนำไปใช้และแก้ไขได้ตามต้องการ

## 🙏 ขอบคุณ

- [QRCode.js](https://github.com/davidshimjs/qrcodejs) - สำหรับสร้าง QR Code
- [Express.js](https://expressjs.com/) - Web Framework
- [Telegram Bot API](https://core.telegram.org/bots/api) - สำหรับส่งข้อความ

---

สร้างด้วย ❤️ โดย Claude Code
