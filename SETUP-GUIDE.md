# 📖 คู่มือการติดตั้งและใช้งาน - ระบบสั่งปลาเผา

## ✨ ฟีเจอร์ทั้งหมด

### สำหรับลูกค้า:
- ✅ สั่งปลาเผาออนไลน์ (รองรับมือถือและ PC)
- ✅ เลือกประเภท: ปลาเผา+น้ำจิ้ม (120 บาท) หรือ ปลาเผา+ชุดผัก+น้ำจิ้ม (180 บาท)
- ✅ เลือกเวลารับสินค้า (09:00-18:00 ทุก 30 นาที)
- ✅ เลือกรับเองหรือจัดส่ง (ส่งฟรีในตำบลอากาศ)
- ✅ ชำระเงินผ่าน QR Code PromptPay หรือโอนเข้าบัญชี
- ✅ อัพโหลดสลิปการโอนเงิน
- ✅ รับแจ้งเตือนสถานะออเดอร์ผ่าน Telegram

### สำหรับร้านค้า (Dashboard):
- ✅ ดูออเดอร์ทั้งหมดแบบ Real-time
- ✅ ดูสถิติออเดอร์ (รอยืนยัน, กำลังทำ, เสร็จแล้ว)
- ✅ ดูรายละเอียดออเดอร์และสลิปการโอนเงิน
- ✅ ปุ่ม "รับออเดอร์" - ยืนยันและแจ้งลูกค้าอัตโนมัติ
- ✅ ปุ่ม "ออเดอร์เสร็จแล้ว" - แจ้งลูกค้าอัตโนมัติ
- ✅ รีเฟรชอัตโนมัติทุก 30 วินาที

## 🚀 ขั้นตอนการติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
cd grilled-fish-order
npm install
```

### 2. ตั้งค่าอีเมลสำหรับแจ้งเตือน

#### สร้าง Gmail App Password:
1. ไปที่ https://myaccount.google.com/security
2. เปิดใช้งาน "2-Step Verification"
3. สร้าง "App Password" สำหรับ Mail
4. **คัดลอก App Password** ที่ได้ (16 ตัวอักษร)

**ดูคู่มือละเอียดใน `EMAIL-SETUP.md`**

### 3. แก้ไขไฟล์ `server.js`

เปิดไฟล์ `server.js` และแก้ไขบรรทัดที่ 26-28:

```javascript
const EMAIL_USER = 'your-email@gmail.com';        // อีเมลที่ใช้ส่ง
const EMAIL_PASS = 'your-app-password';           // App Password 16 ตัวอักษร
const SELLER_EMAIL = 'seller-email@gmail.com';    // อีเมลผู้ขาย (รับแจ้งเตือน)
```

### 4. ตั้งค่า QR Code ชำระเงิน

**สำคัญ!** วางรูป QR Code PromptPay ของคุณลงในโฟลเดอร์:
```
assets/qr-payment.png
```

หรือลูกค้าสามารถโอนเงินเข้าบัญชี:
- **ธนาคาร:** กรุงไทย
- **เลขบัญชี:** 4440565999
- **ชื่อบัญชี:** นรารักษ์ ชารัตน์

### 5. เริ่มใช้งาน

```bash
npm start
```

## 🌐 เปิดใช้งาน

### สำหรับลูกค้า:
เปิดเบราว์เซอร์ไปที่: **http://localhost:3000**

### สำหรับร้านค้า (Dashboard):
เปิดเบราว์เซอร์ไปที่: **http://localhost:3000/dashboard**

## 📋 วิธีใช้งาน

### ฝั่งลูกค้า:

1. **เลือกเมนู:**
   - ปลาเผา+น้ำจิ้ม (120 บาท)
   - ปลาเผา+ชุดผัก+น้ำจิ้ม (180 บาท)

2. **กรอกข้อมูล:**
   - ชื่อ
   - เบอร์โทร
   - เลือกวิธีรับสินค้า: รับเองหรือจัดส่ง
   - เลือกเวลาที่ต้องการรับ
   - ถ้าจัดส่ง: ใส่ที่อยู่หรือลิงก์ Google Map

3. **ชำระเงิน:**
   - กดปุ่ม "สั่งซื้อและชำระเงิน"
   - สแกน QR Code หรือโอนเงินเข้าบัญชี
   - **อัพโหลดสลิปการโอนเงิน** (บังคับ)
   - กดปุ่ม "ยืนยันการชำระเงินและส่งออเดอร์"

4. **รับแจ้งเตือน:**
   - เมื่อร้านรับออเดอร์ → ได้รับการแจ้งเตือนทาง Telegram
   - เมื่อออเดอร์เสร็จ → ได้รับการแจ้งเตือนทาง Telegram

### ฝั่งร้านค้า (Dashboard):

1. **เปิด Dashboard:**
   - ไปที่ http://localhost:3000/dashboard

2. **ดูออเดอร์:**
   - ดูสถิติออเดอร์ด้านบน
   - ดูรายละเอียดออเดอร์แต่ละรายการ
   - คลิกรูปสลิปเพื่อดูภาพใหญ่

3. **จัดการออเดอร์:**
   - **ปุ่ม "รับออเดอร์"** (สีเขียว):
     - กดเมื่อตรวจสอบสลิปแล้วถูกต้อง
     - ระบบจะแจ้งเตือนลูกค้าว่า "ร้านได้รับออเดอร์แล้ว กำลังเตรียมอาหาร"

   - **ปุ่ม "ออเดอร์เสร็จแล้ว"** (สีน้ำเงิน):
     - กดเมื่อทำอาหารเสร็จแล้ว
     - ระบบจะแจ้งลูกค้าว่า "พร้อมรับได้แล้ว" หรือ "กำลังจัดส่ง"

4. **รีเฟรชอัตโนมัติ:**
   - Dashboard จะอัพเดทข้อมูลอัตโนมัติทุก 30 วินาที

## 🔄 สถานะออเดอร์

1. **⏳ รอยืนยัน (pending):**
   - ออเดอร์ใหม่ที่ลูกค้าเพิ่งส่งมา
   - รอร้านกดปุ่ม "รับออเดอร์"

2. **🍳 กำลังทำ (confirmed):**
   - ร้านได้รับออเดอร์แล้ว
   - กำลังเตรียมอาหาร
   - รอร้านกดปุ่ม "ออเดอร์เสร็จแล้ว"

3. **✅ เสร็จแล้ว (completed):**
   - อาหารทำเสร็จแล้ว
   - พร้อมให้ลูกค้ามารับหรือจัดส่ง

## 📁 โครงสร้างไฟล์

```
grilled-fish-order/
├── assets/
│   ├── qr-payment.png       # รูป QR Code ชำระเงิน (ต้องเพิ่มเอง)
│   └── README.txt
├── data/
│   └── orders.json          # ฐานข้อมูลออเดอร์ (สร้างอัตโนมัติ)
├── public/
│   ├── uploads/             # เก็บรูปสลิป (สร้างอัตโนมัติ)
│   ├── index.html           # หน้าสั่งอาหาร
│   ├── payment.html         # หน้าชำระเงิน
│   ├── dashboard.html       # หน้า Dashboard
│   ├── style.css
│   ├── script.js
│   └── payment.js
├── server.js                # Backend Server
├── package.json
└── README.md
```

## 💡 เทคนิคการใช้งาน

### สำหรับลูกค้า:
- แนะนำให้ถ่ายรูปสลิปชัดๆ
- ถ้าจัดส่ง แนะนำให้แนบลิงก์ Google Map เพื่อความแม่นยำ

### สำหรับร้านค้า:
- เปิด Dashboard ไว้ตลอดเวลา จะได้เห็นออเดอร์ใหม่ทันที
- ตรวจสอบสลิปก่อนกดรับออเดอร์เสมอ
- ใช้ Telegram เป็นช่องทางหลักในการแจ้งเตือน

## 🔧 แก้ไขปัญหา

### ไม่ได้รับแจ้งเตือนใน Telegram:
1. ตรวจสอบ Bot Token และ Chat ID ใน `server.js`
2. ลองส่งข้อความไปหาบอทก่อน
3. เช็ค console log ใน Terminal

### รูป QR Code ไม่แสดง:
1. ตรวจสอบว่าวางไฟล์ `qr-payment.png` ในโฟลเดอร์ `assets` แล้ว
2. ชื่อไฟล์ต้องเป็น `qr-payment.png` เท่านั้น

### อัพโหลดสลิปไม่ได้:
1. ตรวจสอบว่าไฟล์เป็นรูปภาพ (.jpg, .png)
2. ขนาดไฟล์ไม่เกิน 10 MB

## 🌟 ข้อมูลร้าน

- **ชื่อร้าน:** อิหล่าปลาเผา
- **สถานที่:** หน้าร้านทองเยาวราชนำโชค
- **พื้นที่บริการ:** ส่งฟรีในตำบลอากาศ
- **เบอร์ติดต่อ:** 093-549-6234
- **เวลาทำการ:** 09:00 - 18:00 น.

## 📞 ช่องทางติดต่อ

- **เบอร์โทร:** 093-549-6234
- **Telegram:** จะได้รับแจ้งเตือนอัตโนมัติ

---

**สร้างด้วย ❤️ โดย Claude Code**
