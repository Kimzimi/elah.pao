# 🚀 วิธีเริ่มใช้งานเว็บสั่งปลาเผา

## ขั้นตอนที่ 1: ติดตั้ง Node.js
ถ้ายังไม่มี Node.js ให้ดาวน์โหลดและติดตั้งจาก: https://nodejs.org/

## ขั้นตอนที่ 2: สร้าง Telegram Bot

### 2.1 สร้างบอท
1. เปิด Telegram และค้นหา **@BotFather**
2. ส่งคำสั่ง: `/newbot`
3. ตั้งชื่อบอท เช่น: `ปลาเผาอากาศ`
4. ตั้ง username เช่น: `akardsGrilledFishBot`
5. **คัดลอก Bot Token** ที่ได้ เช่น: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2.2 หา Chat ID
1. เปิดแชทกับบอทที่สร้างไว้
2. ส่งข้อความ `/start` ไปหาบอท
3. เปิดเบราว์เซอร์ และไปที่ URL นี้ (แทนที่ YOUR_BOT_TOKEN ด้วย token ที่คุณได้):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
```
4. หา `"chat":{"id":123456789}` - ตัวเลขนี้คือ **Chat ID** ของคุณ

## ขั้นตอนที่ 3: ตั้งค่าโปรเจค

### 3.1 เปิด Terminal และติดตั้ง packages
```bash
cd grilled-fish-order
npm install
```

### 3.2 แก้ไขไฟล์ server.js
เปิดไฟล์ `server.js` และแก้ไข:
```javascript
const TELEGRAM_BOT_TOKEN = 'ใส่ Bot Token ของคุณ';
const TELEGRAM_CHAT_ID = 'ใส่ Chat ID ของคุณ';
```

## ขั้นตอนที่ 4: เริ่มใช้งาน

### เปิดเซิร์ฟเวอร์
```bash
npm start
```

### เปิดเบราว์เซอร์
ไปที่: http://localhost:3000

## 🎉 เสร็จแล้ว!

ตอนนี้:
- ลูกค้าสามารถสั่งปลาเผาได้ที่ http://localhost:3000
- ข้อมูลการส่ง: **ส่งฟรีในตำบลอากาศ**
- เบอร์ติดต่อ: **093-549-6234**
- เบอร์ PromptPay: **0935496234** (ตั้งค่าไว้แล้ว)
- ออเดอร์จะส่งเข้า Telegram ของคุณอัตโนมัติ

## 📝 หมายเหตุ

### ถ้าต้องการเปลี่ยนเมนูหรือราคา:
แก้ไขไฟล์:
- `public/index.html` - เปลี่ยนชื่อเมนูและราคา
- `public/script.js` - อัพเดท object `prices` และ `itemNames`

### ถ้าต้องการเปิดให้คนอื่นเข้าได้:
- ใช้ ngrok: `npx ngrok http 3000`
- หรือ deploy ขึ้น Vercel, Railway, Render

## ❓ แก้ไขปัญหา

**ถ้า npm install ไม่ได้:**
- ลองลบโฟลเดอร์ `node_modules` และไฟล์ `package-lock.json`
- รัน `npm install` อีกครั้ง

**ถ้าไม่ได้รับแจ้งเตือนใน Telegram:**
- ตรวจสอบ Bot Token และ Chat ID
- ลองส่งข้อความไปหาบอทก่อน
- ดู console log ว่ามี error อะไร
