# 🤖 คู่มือตั้งค่า Telegram Bot สำหรับแจ้งเตือนออเดอร์

## 📋 ภาพรวม
ระบบจะส่งแจ้งเตือนออเดอร์ใหม่ผ่าน Telegram Bot โดยอัตโนมัติ ทุกครั้งที่มีลูกค้าสั่งซื้อ

## ✅ ขั้นตอนการตั้งค่า

### 1️⃣ สร้าง Telegram Bot
1. เปิดแอป Telegram บนมือถือหรือคอมพิวเตอร์
2. ค้นหา **@BotFather** (ติ๊กถูกสีฟ้า - Verified Account)
3. พิมพ์คำสั่ง `/start` เพื่อเริ่มต้น
4. พิมพ์คำสั่ง `/newbot` เพื่อสร้างบอทใหม่
5. ตั้งชื่อบอท เช่น `อิหล่าปลาเผา แจ้งเตือน`
6. ตั้งชื่อผู้ใช้ (username) ต้องลงท้ายด้วย `bot` เช่น `elah_pao_notify_bot`
7. BotFather จะส่ง **Bot Token** ให้ (ตัวอย่าง: `7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`)
   - ⚠️ **เก็บรักษา Token นี้ไว้ให้ดี อย่าส่งให้ใครเห็น!**

### 2️⃣ หา Chat ID ของคุณ
1. ค้นหาบอทที่คุณเพิ่งสร้าง (ชื่อที่ตั้งในขั้นตอนที่ 6)
2. กด **Start** หรือพิมพ์ `/start` เพื่อเริ่มแชท
3. พิมพ์ข้อความอะไรก็ได้ เช่น `สวัสดี`
4. เปิดเบราว์เซอร์ แล้วไปที่ URL นี้ (แทนที่ `YOUR_BOT_TOKEN` ด้วย Token จากขั้นตอนที่ 1)
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
   ตัวอย่าง:
   ```
   https://api.telegram.org/bot7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw/getUpdates
   ```
5. คุณจะเห็นข้อความ JSON หา `"chat":{"id":123456789}`
   - เลข `123456789` คือ **Chat ID** ของคุณ

### 3️⃣ เพิ่มค่าลง .env.local (สำหรับทดสอบในเครื่อง)
เปิดไฟล์ `.env.local` แล้วแก้ไขบรรทัดท้ายสุด:

```env
TELEGRAM_BOT_TOKEN="7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw"
TELEGRAM_CHAT_ID="123456789"
```

⚠️ **สำคัญ:** ใส่ค่าของคุณเอง อย่าใช้ตัวอย่างข้างบน!

### 4️⃣ เพิ่มค่าลง Vercel (สำหรับ Production)
1. เปิด [Vercel Dashboard](https://vercel.com)
2. เข้าโปรเจค `elah-pao`
3. ไปที่ **Settings** > **Environment Variables**
4. เพิ่มตัวแปร 2 ตัว:

   **ตัวแปรที่ 1:**
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: `7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw` (ใช้ค่าของคุณ)
   - Environment: เลือก **Production**, **Preview**, **Development**
   - กด **Save**

   **ตัวแปรที่ 2:**
   - Name: `TELEGRAM_CHAT_ID`
   - Value: `123456789` (ใช้ค่าของคุณ)
   - Environment: เลือก **Production**, **Preview**, **Development**
   - กด **Save**

5. ทำการ **Redeploy** โปรเจค:
   - ไปที่แท็บ **Deployments**
   - กดปุ่ม **...** (3 จุด) ที่ deployment ล่าสุด
   - เลือก **Redeploy**
   - กด **Redeploy** อีกครั้งเพื่อยืนยัน

### 5️⃣ ทดสอบระบบ
1. สั่งออเดอร์ทดสอบที่ [https://elah-pao.vercel.app](https://elah-pao.vercel.app)
2. คุณจะได้รับแจ้งเตือนทั้ง **อีเมล** และ **Telegram** พร้อมกัน
3. ข้อความแจ้งเตือนจะมีข้อมูล:
   - Order ID
   - ชื่อและเบอร์ลูกค้า
   - เวลาสั่ง / เวลาที่ต้องการรับ
   - รายการสินค้า
   - ยอดรวม
   - ลิงก์ไปยัง Dashboard

## 🔧 แก้ไขปัญหา

### ไม่ได้รับการแจ้งเตือนทาง Telegram
1. ตรวจสอบว่าคุณ **กด Start** ที่บอทแล้วหรือยัง
2. ตรวจสอบว่า **Bot Token** และ **Chat ID** ถูกต้อง
3. ตรวจสอบว่าเพิ่มค่าลง Vercel ครบถ้วนแล้ว
4. ตรวจสอบว่าได้ Redeploy โปรเจคหลังจากเพิ่มค่าแล้ว
5. ดูข้อผิดพลาดที่ Vercel Logs:
   - ไปที่ Vercel Dashboard
   - แท็บ **Deployments** > เลือก deployment ล่าสุด
   - แท็บ **Functions** > ดู Logs

### ต้องการเปลี่ยน Bot Token หรือ Chat ID
1. แก้ไขค่าใน Vercel Environment Variables
2. Redeploy โปรเจค

## 📝 หมายเหตุ
- Telegram Bot แจ้งเตือนฟรี 100% ไม่มีค่าใช้จ่าย
- ระบบจะแจ้งเตือนทั้งอีเมลและ Telegram พร้อมกัน
- ถ้าไม่ตั้งค่า Telegram ระบบจะข้ามไปแจ้งเตือนทางอีเมลอย่างเดียว
